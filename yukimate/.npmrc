# npm configuration for Slope Link

# Use legacy peer deps to resolve React 19 compatibility with iconoir-react-native
legacy-peer-deps=true

# Increase timeout for slow networks
fetch-timeout=60000

# Save exact versions in package.json
save-exact=false
